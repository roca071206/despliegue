# Este módulo era para la tabla Administrador legacy, que fue reemplazada
# por django.contrib.auth.User + usuarios/. Ya no se usa desde urls.py.
# Se mantiene vacío para no romper imports si alguien lo referencia.
